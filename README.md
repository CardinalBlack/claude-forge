# claude-bootstrap

Layered behavior-hardening for Claude Code: hooks, skills, subagents,
templates, and crons that install onto any machine + any project.

See `docs/architecture.md` (Phase 9) for full structure.

## Install

    git clone --recurse-submodules <this-repo> ~/.claude-bootstrap
    cd ~/.claude-bootstrap
    ./bootstrap.sh
